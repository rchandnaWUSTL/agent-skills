# Amazon Q Instructions for Terraform

This repo contains instructions files for the Amazon Q CLI AI agent when working on Terraform providers and configurations.

Currently supported:

- agents

## Usage

1. Copy files from this repo's `.aws/amazonq` directory into your user directory `~/.aws/amazonq`.
2. In `q`, enter `/agent switch terraform-action-agent` to use the agent.
